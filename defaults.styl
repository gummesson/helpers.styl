//-------------
//  Variables
//-------------

// The base spacing unit used through out the margins and paddings
base-spacing-unit = 1.5em unless base-spacing-unit is defined

// The base font size used for the font sizes
base-font-size = 1em unless base-font-size is defined
